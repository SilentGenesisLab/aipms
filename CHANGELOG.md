# 更新日志

本仓库暂无版本号与 Git tag，因此按**日期**记录变更，分类沿用提交信息里的类型；新条目置顶。

分类说明：`新增` 功能特性 · `修复` 缺陷修正 · `优化` 性能与体验 · `运维` 部署与 CI · `文档` 文档与说明。

## 2026-09-20

### 新增

- CLI 更名为 `aipms`（原 `chorify`），所有子命令与帮助文本同步更新。
- 凭据位置调整：配置由 `~/.chorify/config` 改为**执行安装命令的当前目录**下的 `.aipms/config`（权限 600），环境变量由 `CHORIFY_*` 改为 `AIPMS_*`。这样每个工作区可以独立持有一份 Key。
- 新增 Agent Skill 支持：安装器会拉取 `/cli/skill` 并把 `SKILL.md` 写入 `.agents/skills/aipms-project-operations/SKILL.md`，供 AI 工具直接读取项目操作规范。
- 新增 `aipms doctor --json`：以 JSON 输出身份、工作上下文、项目、团队、通知、审计日志六项只读能力探测结果；403 表示当前 Key 缺少该项权限，不做绕过。
- 安装流程改为交互式：不再要求命令行传入 `--api-key`，安装器打开网站，用户在隐藏输入中粘贴 Key，避免密钥进入 shell 历史。
- `/cli/skill` 与 `/cli/install.ps1` 加入公开路由白名单，安装前无需登录即可获取。

### 文档

- README 新增「AIPMS CLI 与 Agent Skill」章节，给出 Linux/macOS 与 Windows 两套安装示例，并说明不要把 `.aipms/config` 提交到 Git。
- `/cli/guide` 使用说明随更名同步更新。

### 已知问题

- Windows 安装入口 `https://aipms.sligenai.cn/cli/install.ps1` 在 README 与 `/cli/guide` 中均有引用，但**对应路由尚未实现**，实际返回 404。当前 Windows 用户无法使用文档给出的安装方式。
- `/cli/install.ps1` 已加入公开白名单，暂时没有实际作用。

## 2026-09-16

### 新增

- 新增 macOS Token 用量采集器，移除对 Node 的运行时依赖。

### 修复

- 修复 JXA 目录枚举的结束判断。
- 采集器校验改用 CommonJS 运行。

### 运维

- CI 增加 macOS 采集器校验任务。

## 2026-09-09

### 新增

- API Key 权限支持一键全选。

## 2026-09-08

### 新增

- 新增跨周项目排期视图，统一需求与任务的排期控件。
- 发布中心补充部署日志与提交详情展示。
- 强化部署中心与告警。

### 修复

- 排期支持拖拽保持、工作项列宽可调、缩放时保持当前日期。
- 排期按自然周期对齐。
- 蓝绿状态标记规范化，预发布更新改为蓝绿安全。
- 无需 GitHub App 即可读取公开提交元数据，并支持从本地检出回填提交信息。

## 2026-09-04

### 新增

- 新增可执行的 CI/CD 发布中心。
- 新增团队 AI 用量分析。
- Token 用量统计并入团队成员档案。

### 修复

- 运行时镜像内置生成的 Prisma Client；精简部署镜像依赖，并直接安装运行时部署依赖。
- 回滚步骤保留、部署结果载荷隔离、受限 SSH 下预载发布镜像。
- Windows 采集器扫描补全，采集器重装更可靠。
- 按年份展示累计工时；兼容集合类型的 token 字段。

## 2026-09-03

### 新增

- 新增项目成员档案与 Token 用量追踪。
- 新增项目周报活动与策略历史。
- 新增任务验收工作流。

### 运维

- 每周项目快照定时任务。

### 文档

- 新增产品 BP 文档并刷新 README。

## 2026-08-31

### 新增

- 支持个人项目及转入团队。
- 新增团队成员档案与详情页。

### 修复

- 允许团队成员创建项目；恢复 API Key 页面兼容性。

## 2026-08-29

### 新增

- 新增完整 API 接口面与可安装的 CLI。

### 修复

- CLI 安装器保留公开访问域名。

## 2026-08-28

### 新增

- 新增项目工作区与文件平台：目录浏览、文件夹上传、全局未分配文件、文件版本与容量限制。
- 新增团队协作中心、统一审计日志中心、成员 OKR 概览与周报。
- 需求、任务与版本管理能力增强；新增个人与委派任务中心。
- 新增安全账户设置、项目资料与删除管理。
- 账户菜单加入主题切换与反馈入口，并补齐深色主题覆盖面。

### 修复

- 团队邀请链接可复用并生成公开 URL。
- 文件接口对受限 API Key 授权；旧数据迁移与容量限制生效。
- 默认头像改用账户首字母。

### 运维

- 预发布环境改为从 `uat` 分支部署；新增私有 MinIO 的 nginx 路由。

## 2026-08-27

### 新增

- 从 Create Next App 初始化项目，搭建 Chorify Projects 预发布应用。
- 新增注册与受保护会话、团队工作流与 AI Native 品牌。
- 新增作用域 API Key 管理。

### 修复

- 校验团队邀请流程；刷新 favicon 为版本化应用图标。
- 撤回过一次「登录时要求同意产品条款」的改动。

### 运维

- 预发布构建改用国内 npm 镜像，并指向已解析的预发布域名。

---

> 说明：以上 2026-08-27 至 2026-09-16 的条目依据提交历史整理；2026-09-20 条目对应提交 `feat: rename CLI to aipms and add agent skill`。
